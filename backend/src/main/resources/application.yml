spring:
  datasource:
    url: jdbc:mysql://localhost:3306/seats-db
    username: seats
    password: seat0310
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  sql:
    init:
      mode: always