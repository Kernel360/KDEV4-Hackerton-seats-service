spring:
  datasource:
    url: jdbc:mysql://localhost:3306/seats-db
    username: seats
    password: seat0310
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
  sql:
    init:
      mode: always